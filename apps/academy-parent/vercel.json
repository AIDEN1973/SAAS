{
  "buildCommand": "cd ../.. && ./node_modules/.bin/turbo run build --filter=@app/academy-parent",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

