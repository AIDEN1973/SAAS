{
  "buildCommand": "cd ../.. && rm -rf .turbo && npx turbo@2 run build --filter=@app/public-gateway",
  "outputDirectory": "dist",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

