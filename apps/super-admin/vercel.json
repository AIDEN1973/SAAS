{
  "buildCommand": "cd ../.. && ./node_modules/.bin/turbo run build --filter=@app/super-admin",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

