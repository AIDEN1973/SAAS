# 코드 검증 지시문 모음 (Code Verification Prompts)

**버전**: 2.0.0
**최종 업데이트**: 2025-01-29
**목적**: 모든 도메인에서 즉시 사용 가능한 범용 검증 지시문 템플릿 제공
**관련 문서**: `docu/체크리스트.md` (검증 기준 참조)
**적용 범위**: 대시보드, 학생관리, 출결관리, 문자발송, 통계분석, 인공지능, 수업관리, 강사관리, 수납관리, 자동화 설정 등 모든 도메인

---

## 📋 빠른 시작

### 가장 많이 사용하는 지시문 Top 3

#### 1️⃣ 신규 코드 전체 검증

```
방금 구현한 코드를 체크리스트.md로 검증해줘:
- P0 항목 (보안/멀티테넌트/데이터 무결성)
- 변경된 파일만 집중 검증
- 오류 발견 시 표로 정리

결과 형식:
| 항목 | 상태 | 파일:라인 | 비고 |
```

#### 2️⃣ 로직 정확성 검증

```
[파일 경로]의 [함수명]을 다음 케이스로 검증해줘:
1. [입력 1] → [기대 출력 1]
2. [입력 2] → [기대 출력 2]
3. [입력 3] → [기대 출력 3]

각 케이스를 코드에서 추적하고 통과/실패 여부를 표로 정리해줘:
| 케이스 | 입력 | 기대 출력 | 실제 출력 | 상태 | 문제 라인 |
```

#### 3️⃣ 기존 코드 재검증

```
다음 파일들을 체크리스트.md P0 항목으로 재검증해줘:
- [파일 1]
- [파일 2]

문제 발견 시 우선순위별로 분류 (P0/P1/P2)하고 표로 정리해줘.
```

---

## 📚 범용 템플릿 (모든 도메인 적용 가능)

### 템플릿 A: 데이터 변환/정규화 로직 검증

**적용 도메인**:
- 학생관리: 학생 정보 정규화, 입력 검증
- 출결관리: 출결 데이터 변환, 상태 정규화
- 문자발송: 메시지 템플릿 처리, 변수 치환
- 수납관리: 청구서 데이터 변환, 금액 정규화
- 통계분석: 집계 데이터 변환, 차트 데이터 포맷
- 대시보드: 카드 데이터 정규화, 위젯 데이터 변환
- 인공지능: 파라미터 추출, 자연어 파싱
- 수업관리: 수업 정보 정규화, 일정 데이터 변환
- 강사관리: 강사 정보 정규화, 급여 데이터 변환
- 자동화: 이벤트 데이터 변환, 조건 정규화

**사용 시점**: 입력 정규화, 데이터 변환, 파싱 로직 구현 후

```
[파일 경로]의 [함수명]을 검증해줘:

1단계 (구조 검증):
- 체크리스트.md P0 항목:
  - PII 마스킹 (maskPII() 사용)
  - 타입 가드 (null/undefined 처리)
  - 에러 처리 (try-catch)

2단계 (로직 검증):
정상 케이스:
1. [입력 1] → [기대 출력 1]
2. [입력 2] → [기대 출력 2]
3. [입력 3] → [기대 출력 3]

필터링 케이스:
4. [잘못된 입력 1] → [기대 출력: 필터링됨]
5. [잘못된 입력 2] → [기대 출력: 필터링됨]

엣지 케이스:
6. "" (빈 문자열) → [기대 출력]
7. null → [기대 출력]
8. 경계값 → [기대 출력]

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘:
| 케이스 | 입력 | 기대 출력 | 실제 출력 | 상태 | 문제 라인 |

3단계 (변환 로직 검증):
- 변환 규칙 정확성 확인
- 예외 처리 확인
- 성능 최적화 가능성 확인
```

---

### 템플릿 B: 데이터 조회/필터링 로직 검증

**적용 도메인**:
- 학생관리: 학생 목록 조회, 검색, 필터링
- 출결관리: 출결 기록 조회, 기간별 필터
- 문자발송: 발송 대상 필터링, 수신자 조회
- 수납관리: 청구서 목록, 결제 상태 필터
- 통계분석: 기간별 통계 조회, 집계 데이터 필터
- 대시보드: 카드 데이터 조회, 위젯 필터링
- 수업관리: 수업 목록 조회, 일정 필터
- 강사관리: 강사 목록 조회, 담당 수업 필터
- 인공지능: 이름 → UUID 변환 (Resolver Gate)
- 자동화: 이벤트 조회, 조건 필터링

**사용 시점**: 목록 조회, 검색, 필터링, Resolver 로직 구현 후

```
[파일 경로]의 [함수명]을 검증해줘:

1단계 (구조 검증):
- 체크리스트.md P0 항목:
  - RLS (withTenant() 사용)
  - PII 마스킹 (maskPII() 사용)
  - 에러 처리

2단계 (로직 검증):
정상 케이스:
1. [조회 조건 1] → [기대 결과: 데이터 반환]
2. [조회 조건 2] → [기대 결과: 데이터 반환]

빈 결과 케이스:
3. [존재하지 않는 조건] → [기대 결과: 빈 배열/null]

필터링 케이스:
4. [필터 조건 1] → [기대 결과: 필터링된 데이터]
5. [필터 조건 2] → [기대 결과: 필터링된 데이터]

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘:
| 케이스 | 입력 | 기대 결과 | 실제 결과 | 상태 | 문제 라인 |

3단계 (성능 검증):
- 쿼리 최적화 확인 (인덱스 사용)
- N+1 문제 확인
- 페이지네이션 확인
```

---

### 템플릿 C: 데이터 저장/수정 로직 검증

**적용 도메인**:
- 학생관리: 학생 등록, 정보 수정, 상태 변경
- 출결관리: 출결 기록, 수정, 삭제
- 문자발송: 발송 기록 저장, 결과 업데이트
- 수납관리: 청구서 생성, 결제 처리, 상태 변경
- 상담일지: 상담 기록 저장, 수정
- 수업관리: 수업 등록, 수정, 삭제
- 강사관리: 강사 등록, 정보 수정
- 자동화: 설정 저장, 이벤트 기록
- 대시보드: 카드 설정 저장
- 통계분석: 집계 데이터 저장

**사용 시점**: 생성, 수정, 삭제, 배치 처리 로직 구현 후

```
[파일 경로]의 [함수명]을 검증해줘:

1단계 (구조 검증 - 체크리스트.md P0):
보안:
- [ ] RLS (withTenant() 사용)
- [ ] 트랜잭션/원자성 (필요 시)
- [ ] 멱등성 처리 (중복 방지)

데이터 무결성:
- [ ] 필수 필드 검증
- [ ] 타입 검증
- [ ] 제약 조건 확인

PII:
- [ ] maskPII() 사용 (로그/모니터링)

2단계 (로직 검증):
정상 케이스:
1. [입력 1] → [기대 결과: 저장 성공]
2. [입력 2] → [기대 결과: 수정 성공]

검증 실패 케이스:
3. [잘못된 입력 1] → [기대 결과: 검증 오류]
4. [필수 필드 누락] → [기대 결과: 검증 오류]

중복 방지 케이스:
5. [동일 요청 재시도] → [기대 결과: 멱등성 처리]

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘.

3단계 (통합 검증):
- DB 제약 조건 확인
- 트랜잭션 범위 확인
- 롤백 처리 확인
```

---

### 템플릿 D: 비즈니스 규칙/계산 로직 검증

**적용 도메인**:
- 출결관리: 출석률 계산, 지각/결석 판정
- 수납관리: 청구 금액 계산, 할인 적용, 수수료 계산
- 통계분석: 집계 계산, 추세 분석, 비율 계산
- 인공지능: 위험 점수 계산, AI 브리핑 생성
- 강사관리: 급여 계산, 수익 배분
- 자동화: 임계값 판정, 조건 평가
- 대시보드: 지표 계산, 트렌드 계산
- 수업관리: 수강료 계산, 정원 확인

**사용 시점**: 계산, 집계, 검증, 승인 로직 구현 후

```
[파일 경로]의 [함수명]을 검증해줘:

1단계 (구조 검증):
- 체크리스트.md P0 항목:
  - 타입 검증
  - 에러 처리
  - 엣지 케이스 처리

2단계 (로직 검증):
정상 케이스:
1. [입력 1] → [기대 결과: 계산값]
2. [입력 2] → [기대 결과: 계산값]
3. [입력 3] → [기대 결과: 계산값]

경계값 케이스:
4. 0 → [기대 결과]
5. 최대값 → [기대 결과]
6. 최소값 → [기대 결과]

예외 케이스:
7. null/undefined → [기대 결과: 기본값 또는 에러]
8. 음수 → [기대 결과]
9. 소수점 → [기대 결과]

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘:
| 케이스 | 입력 | 기대 결과 | 실제 결과 | 상태 | 문제 라인 |

3단계 (비즈니스 규칙 검증):
- 계산 공식 정확성 확인
- 반올림/절사 규칙 확인
- 예외 처리 확인
```

---

### 템플릿 E: 정규식/파싱 로직 검증

**적용 도메인**:
- 인공지능: 자연어 파라미터 추출 (ChatOps)
- 문자발송: 메시지 템플릿 파싱, 변수 치환
- 학생관리: 입력 검증 (전화번호, 이메일, 주소)
- 자동화: 조건 표현식 파싱
- 수납관리: 금액 포맷 파싱

**사용 시점**: 정규식, 파싱, 패턴 매칭 로직 구현 후

```
[파일 경로]의 정규식 로직을 검증해줘:

1단계 (패턴 분석):
- 정규식 패턴: [패턴 복사]
- 캡처 그룹 개수: [개수]
- 각 그룹의 의미: [설명]

2단계 (로직 검증):
매칭 성공 케이스:
1. "[입력 1]" → 그룹 1: [값], 그룹 2: [값]
2. "[입력 2]" → 그룹 1: [값], 그룹 2: [값]
3. "[입력 3]" → 그룹 1: [값], 그룹 2: [값]

매칭 실패 케이스:
4. "[입력 4]" → null (매칭 안 됨)
5. "[입력 5]" → null (매칭 안 됨)

엣지 케이스:
6. "" (빈 문자열) → null
7. "   " (공백만) → null
8. 특수문자 포함 → [기대 결과]

각 케이스를 실제 정규식으로 테스트하고 결과를 표로 정리해줘:
| 케이스 | 입력 | 기대 결과 | 실제 결과 | 상태 |

3단계 (정규식 개선):
- 캡처 그룹 순서 최적화
- 이스케이프 문자 처리 개선
- 성능 최적화 (백트래킹 최소화)
```

---

### 템플릿 F: 조건 분기/상태 전환 로직 검증

**적용 도메인**:
- 출결관리: 출석 상태 전환 (출석/지각/결석)
- 수납관리: 결제 상태 전환 (미납/부분납/완납)
- 학생관리: 재원 상태 전환 (재원/퇴원/휴원)
- 자동화: 이벤트 조건 판정, 임계값 체크
- 인공지능: AI 추천 조건, 위험도 분류
- 문자발송: 발송 조건 판정
- 강사관리: 근무 상태 전환

**사용 시점**: 복잡한 조건 분기, 상태 전환 로직 구현 후

```
[파일 경로]의 [함수명] 조건문 로직을 검증해줘:

1단계 (분기 분석):
- 전체 분기 개수: [개수]
- 각 분기 조건: [조건 나열]
- 분기 우선순위: [순서]

2단계 (로직 검증):
각 분기별 테스트:
1. 조건 A 만족 → [기대 결과]
2. 조건 B 만족 → [기대 결과]
3. 조건 C 만족 → [기대 결과]
4. 모든 조건 불만족 → [기대 결과] (else 분기)

경계값 테스트:
5. 조건 경계값 (>=, <=) → [기대 결과]
6. null/undefined → [기대 결과]
7. 빈 배열/객체 → [기대 결과]

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘:
| 케이스 | 입력 | 조건 만족 | 실행 분기 | 기대 결과 | 실제 결과 | 상태 |

3단계 (로직 개선):
- 조건 순서 최적화 (우선순위 높은 것부터)
- 누락된 케이스 확인
- Early return 패턴 적용 가능 여부
```

---

## 🎓 도메인별 실전 예시

### 1. 대시보드 (Dashboard)

#### 예시 1-1: 카드 데이터 정규화 로직

```
apps/academy-admin/src/utils/dashboard-card-normalization.ts의
normalizeDashboardCards 함수를 검증해줘:

1단계 (구조 검증):
- 체크리스트.md P0 항목:
  - 타입 가드 (ensureArray() 사용)
  - 에러 처리

2단계 (로직 검증):
정상 케이스:
1. [카드 배열] → 정규화된 카드 배열
2. 필수 필드 누락 카드 → 기본값 적용
3. 잘못된 타입 → 타입 변환

엣지 케이스:
4. [] (빈 배열) → []
5. null → []
6. undefined → []

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘.

3단계 (정규화 규칙 검증):
- 필수 필드 기본값 확인
- 타입 변환 규칙 확인
- 데이터 무결성 확인
```

#### 예시 1-2: 위젯 렌더링 로직

```
apps/academy-admin/src/utils/dashboardCardRenderer.tsx의
renderCard 함수를 검증해줘:

- 체크리스트.md P0: React Hooks 규칙, Design Tokens 사용
- 로직 검증: 카드 타입별 렌더링, 에러 바운더리
```

---

### 2. 학생관리 (Students)

#### 예시 2-1: 학생 등록 로직

```
[학생 등록 Service 파일]의 createStudent 함수를 검증해줘:

1단계 (구조 검증 - 체크리스트.md P0):
- RLS (withTenant() 사용 금지, tenant_id 직접 포함)
- 데이터 무결성 (필수 필드 검증)
- PII 마스킹 (로그에 maskPII() 사용)

2단계 (로직 검증):
정상 케이스:
1. 모든 필수 필드 → 등록 성공
2. 선택 필드 포함 → 등록 성공

검증 실패 케이스:
3. 필수 필드 누락 → 검증 오류
4. 잘못된 전화번호 형식 → 검증 오류
5. 중복 학생 → 중복 오류

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘.
```

#### 예시 2-2: 학생 검색 로직

```
[학생 검색 Service 파일]의 searchStudents 함수를 검증해줘:

- 체크리스트.md P0: RLS (withTenant() 사용), PII 마스킹
- 로직 검증: 이름 검색, 전화번호 검색, 필터링
```

---

### 3. 출결관리 (Attendance)

#### 예시 3-1: 출결 기록 로직

```
[출결 기록 Service 파일]의 recordAttendance 함수를 검증해줘:

- 체크리스트.md P0: RLS, 멱등성 처리 (중복 기록 방지)
- 로직 검증: 출석/지각/결석 기록, 시간 검증
```

#### 예시 3-2: 출석률 계산 로직

```
[출석률 계산 함수]를 검증해줘:

1단계 (구조 검증):
- 타입 검증, 에러 처리

2단계 (로직 검증):
정상 케이스:
1. 출석 10회, 전체 10회 → 100%
2. 출석 7회, 전체 10회 → 70%

경계값 케이스:
3. 출석 0회, 전체 10회 → 0%
4. 출석 0회, 전체 0회 → 0% (또는 null)

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘.
```

---

### 4. 문자발송 (Notifications)

#### 예시 4-1: 발송 대상 필터링 로직

```
[발송 대상 필터링 함수]를 검증해줘:

- 체크리스트.md P0: RLS, PII 마스킹
- 로직 검증: 조건별 필터링, 중복 제거
```

#### 예시 4-2: 메시지 템플릿 처리 로직

```
[템플릿 처리 함수]를 검증해줘:

1단계 (구조 검증):
- 타입 검증, 에러 처리

2단계 (로직 검증):
정상 케이스:
1. "안녕하세요 {{name}}님" + {name: "박소영"} → "안녕하세요 박소영님"
2. "{{date}} 출결" + {date: "2025-01-29"} → "2025-01-29 출결"

엣지 케이스:
3. 변수 없음 → 원본 반환
4. 변수 누락 → 빈 문자열 또는 기본값

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘.
```

---

### 5. 통계분석 (Analytics)

#### 예시 5-1: 집계 로직

```
[통계 집계 함수]를 검증해줘:

- 체크리스트.md P0: RLS, 성능 최적화
- 로직 검증: 기간별 집계, 그룹별 집계
```

#### 예시 5-2: 차트 데이터 변환 로직

```
[차트 데이터 변환 함수]를 검증해줘:

1단계 (구조 검증):
- 타입 검증, 에러 처리

2단계 (로직 검증):
정상 케이스:
1. DB 데이터 → 차트 포맷 변환
2. 빈 데이터 → 빈 배열

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘.
```

---

### 6. 인공지능 (AI)

#### 예시 6-1: 파라미터 추출 로직 (ChatOps)

```
infra/supabase/supabase/functions/chatops/handlers/intent-resolver.ts의
deriveMinimalParamsFromMessage 함수를 검증해줘:

1단계 (구조 검증):
- 체크리스트.md P0 항목:
  - PII 마스킹 (maskPII() 사용)
  - 타입 가드 (null/undefined 처리)

2단계 (로직 검증):
정상 케이스:
1. "박소영 전화번호" → { name: "박소영" }
2. "학생 박소영 조회" → { name: "박소영" }
3. "고객 이영희 정보" → { name: "이영희" }

키워드 필터링:
4. "전화번호 조회" → { name: undefined }
5. "학생 조회" → { name: undefined }

엣지 케이스:
6. "" (빈 문자열) → { name: undefined }
7. null → { name: undefined }

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘.

3단계 (정규식 검증):
- nameHintPatterns 배열의 각 패턴 확인
- 캡처 그룹 순서 확인
- invalidKeywords 배열 완전성 확인
```

#### 예시 6-2: Resolver Gate 로직

```
infra/supabase/supabase/functions/_shared/normalize-params.ts의
resolveStudentIdByName 함수를 검증해줘:

1단계 (구조 검증):
- 체크리스트.md P0: RLS, PII 마스킹, 에러 처리

2단계 (로직 검증):
1. "존재하지않는이름" → { status: 'not_found' }
2. "박소영" (1명) → { status: 'one', student_id: 'uuid' }
3. "김철수" (2명 이상) → { status: 'many', candidates: [...] }
4. "전화번호" → 키워드 감지, Resolver 스킵

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘.
```

#### 예시 6-3: AI 위험 점수 계산 로직

```
[AI 위험 점수 계산 함수]를 검증해줘:

- 체크리스트.md P0: 타입 검증, 에러 처리
- 로직 검증: 점수 계산 공식, 경계값 처리
```

---

### 7. 수업관리 (Classes)

#### 예시 7-1: 수업 등록 로직

```
[수업 등록 Service 파일]의 createClass 함수를 검증해줘:

- 체크리스트.md P0: RLS, 데이터 무결성
- 로직 검증: 필수 필드 검증, 중복 체크
```

#### 예시 7-2: 수업 일정 계산 로직

```
[수업 일정 계산 함수]를 검증해줘:

1단계 (구조 검증):
- 타입 검증, 에러 처리

2단계 (로직 검증):
정상 케이스:
1. 매주 월/수/금 → 일정 생성
2. 격주 화/목 → 일정 생성

엣지 케이스:
3. 공휴일 제외 → 일정 스킵
4. 종료일 초과 → 일정 생성 중단

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘.
```

---

### 8. 강사관리 (Teachers)

#### 예시 8-1: 강사 등록 로직

```
[강사 등록 Service 파일]의 createTeacher 함수를 검증해줘:

- 체크리스트.md P0: RLS, 데이터 무결성, PII 마스킹
- 로직 검증: 필수 필드 검증, 중복 체크
```

#### 예시 8-2: 급여 계산 로직

```
[급여 계산 함수]를 검증해줘:

1단계 (구조 검증):
- 타입 검증, 에러 처리

2단계 (로직 검증):
정상 케이스:
1. 기본급 + 수당 → 총 급여
2. 기본급 - 공제 → 실수령액

경계값 케이스:
3. 기본급 0 → 0
4. 음수 입력 → 에러

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘.
```

---

### 9. 수납관리 (Billing)

#### 예시 9-1: 청구서 생성 로직

```
[청구서 생성 Service 파일]의 createInvoice 함수를 검증해줘:

- 체크리스트.md P0: RLS, 트랜잭션, 멱등성
- 로직 검증: 금액 계산, 할인 적용, 중복 방지
```

#### 예시 9-2: 결제 처리 로직

```
[결제 처리 함수]를 검증해줘:

1단계 (구조 검증 - 체크리스트.md P0):
- RLS, 트랜잭션, 멱등성 (중복 결제 방지)

2단계 (로직 검증):
정상 케이스:
1. 결제 요청 → 결제 성공, 상태 변경
2. 부분 결제 → 부분납 상태

예외 케이스:
3. 결제 실패 → 상태 유지, 에러 기록
4. 중복 요청 → 멱등성 처리

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘.
```

#### 예시 9-3: 청구 금액 계산 로직

```
[청구 금액 계산 함수]를 검증해줘:

- 체크리스트.md P0: 타입 검증, 에러 처리
- 로직 검증: 수강료 + 할인 - 공제 = 청구 금액
```

---

### 10. 자동화 설정 (Automation)

#### 예시 10-1: 이벤트 조건 판정 로직

```
[이벤트 조건 판정 함수]를 검증해줘:

1단계 (구조 검증):
- 타입 검증, 에러 처리

2단계 (로직 검증):
정상 케이스:
1. 임계값 초과 → true
2. 임계값 미만 → false

경계값 케이스:
3. 임계값 정확히 일치 → true/false (정책 확인)

각 케이스를 코드에서 추적하고 결과를 표로 정리해줘.
```

#### 예시 10-2: 자동화 설정 저장 로직

```
[자동화 설정 저장 함수]를 검증해줘:

- 체크리스트.md P0: RLS, 데이터 무결성
- 로직 검증: 필수 필드 검증, 정책 검증
```

---

## 💡 사용 팁

### 1. 템플릿 커스터마이징

템플릿을 복사해서 프로젝트에 맞게 수정:
- `[파일 경로]`, `[함수명]` 등을 실제 값으로 교체
- 테스트 케이스를 프로젝트 요구사항에 맞게 조정
- 검증 항목을 추가/제거

### 2. 검증 범위 조절

토큰 절약을 위해 검증 범위 조절:
```
# P0만 검증 (가장 중요)
"체크리스트.md P0 항목만 검증해줘"

# 변경된 라인만 검증
"[파일 경로]의 [라인 범위]만 검증해줘"

# 특정 함수만 검증
"[함수명] 함수만 검증해줘"
```

### 3. 결과 형식 지정

검증 결과를 표로 받으면 가독성 향상:
```
"결과를 다음 형식의 표로 정리해줘:
| 항목 | 상태 | 파일:라인 | 비고 |"
```

### 4. 우선순위별 검증

시간이 부족할 때 우선순위별로 검증:
```
# 1순위: P0 (보안/멀티테넌트)
"체크리스트.md P0 항목만 검증해줘"

# 2순위: 로직 정확성
"핵심 로직만 테스트 케이스로 검증해줘"

# 3순위: P1/P2 (코드 품질)
"시간이 되면 P1/P2 항목도 검증해줘"
```

---

## 🔗 관련 문서

- **검증 기준**: `docu/체크리스트.md` (P0/P1/P2 항목)
- **계약 붕괴 방지**: `docu/계약붕괴방지.md`
- **사전 예방**: `docu/붕괴사전예방.md`
- **검증 스크립트**: `scripts/precise-verification.ts`, `scripts/deep-verification.ts`

---

## 📝 변경 이력

### v2.0.0 (2025-01-29)
- **범용화 완료**: ChatOps 특화 → 10개 도메인 모두 적용 가능
- **도메인별 예시 추가**: 대시보드, 학생관리, 출결관리, 문자발송, 통계분석, 인공지능, 수업관리, 강사관리, 수납관리, 자동화 설정
- **템플릿 재구성**: 6개 범용 템플릿 (데이터 변환, 조회, 저장, 비즈니스 규칙, 정규식, 조건 분기)
- **적용 도메인 명시**: 각 템플릿마다 적용 가능한 모든 도메인 나열

### v1.0.0 (2025-01-29)
- 초기 문서 생성 (ChatOps 특화)

---

**이 문서는 복사-붙여넣기용 템플릿 모음입니다.**
**검증 기준은 `docu/체크리스트.md`를 참조하세요.**
**모든 도메인(대시보드, 학생관리, 출결관리, 문자발송, 통계분석, 인공지능, 수업관리, 강사관리, 수납관리, 자동화 설정)에서 사용 가능합니다.**

